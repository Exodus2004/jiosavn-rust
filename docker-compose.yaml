services:
  app:
    container_name: jiosaavn-api-rust
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8787:8787
    environment:
      - RUST_LOG=info
    restart: unless-stopped
